apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: smartgrid-lessee
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: smartgrid-lessee
    spec:
      containers:
      - name: smartgrid-lessee
        image: 10.0.2.62:5000/gridvo/smartgrid-lessee:dev
        imagePullPolicy: Always
        ports:
        - containerPort: 3001
        env:
        - name: ZK_TRACER_HTTP
          value: "true"
        - name: IS_DEBUG
          value: "true"
             
